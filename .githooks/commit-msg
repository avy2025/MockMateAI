#!/bin/sh
# Remove Cursor agent co-author trailer if injected into the message.
if [ -f "$1" ]; then
  grep -v -E '^Co-authored-by: Cursor <cursoragent@cursor.com>$' "$1" > "$1.tmp"
  mv "$1.tmp" "$1"
fi
exit 0
